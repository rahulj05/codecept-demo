{
  "dependencies": {
    "codeceptjs": "^3.0.2"
  },
  "devDependencies": {
    "playwright": "^1"
  },
  "scripts": {
    "all":  "npx codeceptjs run",
    "smoke":  "npx codeceptjs run --grep smoke",
    "run_suite": "npx codeceptjs run --grep $npm_config_suite"
  }
}
